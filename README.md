# Git Complexity Report

Ce projet génère un rapport PDF de complexité par client à partir d’un fichier JSON listant des dépôts Git.  
Utilise TypeScript, `cloc`, `pdfkit` et `chart.js`.

## Installation

```bash
npm install
